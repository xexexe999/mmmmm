using UnityEngine;
using System.Collections;

public class triggerdemo : MonoBehaviour {

	// Use this for initialization
	void OnTriggerEnter ( Collider activator)
    { if ( activator.tag == "Player") { //wtv u want to happen, or nothing}


    }
	
	}
}
